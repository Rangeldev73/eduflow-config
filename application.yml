spring:
  cloud:
    refresh:
      extra-refreshable: none
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
  
  rabbitmq:
    host: eduflow-rabbitmq
    port: 5672
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASSWORD}
    template:
      observation-enabled: true
    listener:
      simple:
        observation-enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,circuitbreakers,busrefresh
  tracing:
    sampling:
      probability: 1.0
    export:
      zipkin:
        endpoint: http://eduflow-zipkin:9411/api/v2/spans

eureka:
  client:
    service-url:
      defaultZone: http://eduflow-eureka-server:8761/eureka
    refresh:
      enable: false
  instance:
    prefer-ip-address: true

platform:
  announcement: "Spring Cloud Bus Test!!"